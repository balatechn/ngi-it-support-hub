services:
  app:
    build: .
    expose:
      - "3000"
    volumes:
      - /data/ngi-tickets:/data
    restart: unless-stopped
    networks:
      - coolify

networks:
  coolify:
    external: true
