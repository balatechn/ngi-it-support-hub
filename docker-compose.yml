services:
  app:
    build: .
    expose:
      - "3000"
    volumes:
      - /data/ngi-tickets:/data
    restart: unless-stopped
    networks:
      - coolify
    labels:
      - "traefik.docker.network=coolify"

networks:
  coolify:
    external: true
